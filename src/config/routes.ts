const routes = {
    dashboard: '/',
    managerTicket: '/manager-ticket',
    exchange: '/exchange',
    list: '/list-event',
    managerDevice: '/manager-device',
    settings: '/settings',
};

export default routes;
